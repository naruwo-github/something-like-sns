{
  "name": "something-like-sns",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "turbo": "^2.0.4"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "lint:biome": "biome lint .",
    "format:biome": "biome format --write ."
  }
}
